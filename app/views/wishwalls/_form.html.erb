<%= form_for(@wishwall) do |f| %>
  <% if @wishwall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wishwall.errors.count, "error") %> prohibited this wishwall from being saved:</h2>

      <ul>
      <% @wishwall.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :wish %><br />
    <%= f.text_field :wish %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
